# GradientFlow Environment Variables
# Copy this file to .env and fill in the values

# =============================================================================
# Server Configuration
# =============================================================================
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# =============================================================================
# Security (MUST change in production!)
# =============================================================================
JWT_SECRET=your-secure-jwt-secret-here
AGENT_API_TOKEN=your-agent-api-token-here

# =============================================================================
# RAG Service
# =============================================================================
# Local development:
# RAG_SERVICE_URL=http://localhost:4001
# Production (Railway):
RAG_SERVICE_URL=""

# =============================================================================
# LLM Configuration (optional - can also configure per-agent in UI)
# =============================================================================
# LLM_ENDPOINT=https://api.openai.com/v1
# LLM_API_KEY=your-api-key-here
# LLM_MODEL=gpt-4

# =============================================================================
# Database
# =============================================================================
# DB_PATH=server/data.json

# =============================================================================
# Railway Deployment Notes
# =============================================================================
#
# This project has 3 Railway services:
#
# 1. RAG Service (agents/rag/)
#    - Already deployed at: gradientflow-production.up.railway.app
#    - Root Directory: agents/rag
#
# 2. Frontend + Backend (project root)
#    - Root Directory: . (or leave empty)
#    - Required env vars:
#      NODE_ENV=production
#      JWT_SECRET=<secure-secret>
#      AGENT_API_TOKEN=<token>
#      CLIENT_ORIGIN=https://<your-web-domain>.up.railway.app
#      RAG_SERVICE_URL=https://gradientflow-production.up.railway.app
#
# 3. Agent Runner (agents/)
#    - Root Directory: agents
#    - Required env vars:
#      API_BASE=https://<your-web-domain>.up.railway.app
#      AGENT_API_TOKEN=<same-token-as-backend>
#      AGENT_LOGIN_EMAIL=root@example.com
#      AGENT_LOGIN_PASSWORD=<password>
